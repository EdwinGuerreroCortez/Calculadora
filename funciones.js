const suma = (v1, v2) => v1 + v2;
const resta = (v1, v2) => v1 - v2;
const multiplicacion = (v1, v2) => v1 * v2;
const division = (v1, v2) => (v2 === 0 ? "Error" : v1 / v2);

export	{suma,resta,multiplicacion,division};

